# Bot framework (aiogram v3 is now used)
aiogram==3.2.0

# Web server
Flask[async]==2.2.5
flask-cors==3.0.10
gunicorn==20.1.0

# Database connector (binary version to avoid build issues on Windows)
psycopg2-binary==2.9.5

# Environment variables
python-dotenv==0.21.0

# HTTP client
requests==2.28.1
